export default class Product {
  constructor({ image, title, id, brand, model, color, price, discount }) {
    this.image = image;
    this.title = title;
    this.id = id;
    this.brand = brand;
    this.model = model;
    this.color = color;
    this.price = price;
    this.discout = discount;
  }
}
